# Environment Variables for Data Migration
# Copy this file to .env.local before running migrations

# ==============================================================
# OPÇÃO 1: Service Account Key (Recomendado para Produção)
# ==============================================================
# Baixe o arquivo JSON da service account do Firebase Console
# (Settings > Service Accounts > Generate New Private Key)
# Cole o conteúdo completo aqui (uma única linha):
FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account","project_id":"seu-projeto",...}'

# ==============================================================
# OPÇÃO 2: Project ID (Mais simples para desenvolvimento)
# ==============================================================
# Se você já está autenticado com gcloud CLI ou Firebase CLI,
# pode usar apenas o Project ID:
NEXT_PUBLIC_FIREBASE_PROJECT_ID=seu-projeto-id

# ==============================================================
# Outras variáveis do Firebase (necessárias para o app)
# ==============================================================
NEXT_PUBLIC_FIREBASE_API_KEY=sua_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=seu_projeto.firebaseapp.com
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=seu_projeto.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abc123

# ==============================================================
# COMO USAR:
# ==============================================================
# 1. Copie este arquivo:
#    cp .env.migration.example .env.local
#
# 2. Preencha as credenciais do Firebase
#
# 3. Carregue as variáveis (Linux/Mac):
#    export $(cat .env.local | xargs)
#
#    Windows (PowerShell):
#    Get-Content .env.local | ForEach-Object {
#      $name, $value = $_.split('=')
#      Set-Content env:\$name $value
#    }
#
# 4. Execute a migração:
#    npm run migrate:jf

# =============================================================
# Painel Admin — /administradores
# =============================================================
# Copie para .env.admin.local e configure suas credenciais.
# Este arquivo é commitado (exemplo). O .env.admin.local é ignorado.
#
# EM DESENVOLVIMENTO: sem configuração necessária.
# Acesse /administradores/login com:  dev / dev
#
# EM PRODUÇÃO: defina as três variáveis abaixo (no .env.local ou
# nas variáveis de ambiente da Vercel/host).
# =============================================================

ADMIN_USERNAME=admin
ADMIN_PASSWORD=troque-por-senha-forte

# Gere o secret com:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_JWT_SECRET=cole-aqui-64-chars-hex
